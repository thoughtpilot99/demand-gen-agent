{
  "mcpServers": {
    "metadataone": {
      "type": "http",
      "url": "https://mcp-server.metadata.io/mcp",
      "headers": {
        "Authorization": "Bearer ${METADATAONE_TOKEN}"
      }
    },
    "slack": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_TEAM_ID": "${SLACK_TEAM_ID}"
      }
    }
  }
}
